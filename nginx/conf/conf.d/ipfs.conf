server {
    listen 80;
    listen [::]:80;
    server_name ipfs.itsnot.fun;

    # 直接反向代理到本地 IPFS 网关
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_redirect off;
    }

    # 禁止直接访问 API 端口（5001）
    location /api/ {
        return 403;
    }

    client_max_body_size 0;  # 禁用文件大小限制（适合大文件传输）
}

# 以下为 443 端口相关配置，已全部注释
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name ipfs.itsnot.fun;
#
#     # SSL 证书配置
#     # ssl_certificate /path/to/your/cert.pem;
#     # ssl_certificate_key /path/to/your/key.pem;
#
#     # 反向代理到本地 IPFS 网关
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#
#         # 支持 IPFS 网关的长路径（如 /ipfs/QmHash...）
#         proxy_http_version 1.1;
#         proxy_set_header Connection "";
#         proxy_buffering off;
#         proxy_redirect off;
#     }
#
#     # 性能优化
#     client_max_body_size 0;  # 禁用文件大小限制（适合大文件传输）
# }
# server {
#     listen 443 ssl http2;
#     server_name ipfs.itsnot.fun;
#
#     # ssl_certificate /path/to/your/cert.pem;
#     # ssl_certificate_key /path/to/your/key.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:5001;
#         proxy_set_header Host $host;
#
#         # 严格限制 API 访问（按需开放）
#         allow 192.168.1.0/24;  # 仅允许内网访问
#         deny all;
#
#         # 启用 Basic Auth（额外保护）
#         auth_basic "IPFS API Admin";
#         auth_basic_user_file /etc/nginx/.htpasswd;
#     }
# }